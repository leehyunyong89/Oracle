Connecting Locally using OS Authentication (로컬로 연결하기)

<방법>
When you install Oracle Database XE, 
your Windows user is automatically added to the ORA_DBA operating system group, which grants you the SYSDBA privileges. 
You can use the following commands to connect to the database. 
In the commands, replace "<oracle_home>" with the path to your Oracle Home.


==========커맨드라인 입력 내용============

cd <oracle_home>\bin
sqlplus / as sysdba
